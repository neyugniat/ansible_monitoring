---
- name: Template elastic-agent.yml
  ansible.builtin.template:
    src: elastic-agent.yml.j2
    dest: /etc/elastic-agent/elastic-agent.yml
    owner: root
    group: root
    mode: "0600"
    backup: true
  notify: Restart Elastic Agent

groups:
  - name: node

    rules:
      - alert: NodeDown
        expr: up == 0
        for: 1m

        labels:
          severity: critical

        annotations:
          summary: Node is down
          description: "{{ '{{' }} $labels.instance {{ '}}' }} is unreachable."